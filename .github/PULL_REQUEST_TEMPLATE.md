## What this changes

<!-- What does a reader of the books gain? One or two sentences. -->

## Checklist

- [ ] I edited the `beyond-*/` book source, not `forgeloop/` directly
- [ ] `make check` passes locally (ruff, notebook guard, assembly, tests)
- [ ] Notebook output is stripped (`python scripts/notebook_outputs.py --strip`)
- [ ] No secrets, license keys, trained weights, or real data added
